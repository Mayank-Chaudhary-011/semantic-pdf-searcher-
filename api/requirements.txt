fastapi>=0.138.0
uvicorn>=0.49.0
python-multipart>=0.0.12
pydantic>=2.0.0
python-dotenv>=1.0.0
# IMPORTANT: must be PyJWT (not `jwt` or `python-jwt` — they have a different API)
PyJWT[crypto]>=2.8.0
sentence-transformers>=5.0.0
supabase>=2.0.0
